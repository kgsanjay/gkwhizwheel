/**
 * Mobile App Version Configuration.
 * Sourced from package.json / build settings for internal sideloaded APK distribution.
 */

export const CURRENT_APP_VERSION = '1.0.0';
export const CURRENT_BUILD_NUMBER = 10;
export const APP_CHANNEL = 'internal_staff_android';

export default {
    CURRENT_APP_VERSION,
    CURRENT_BUILD_NUMBER,
    APP_CHANNEL,
};
